{
  "name": "sqrt-ledger-function-app",
  "version": "1.0.0",
  "description": "The Open-Source API-First Ledger",
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write --config prettier.config.js --list-different **/*.ts",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "watch": "tsc -w"
  },
  "dependencies": {
    "joi": "^17.4.3",
    "mongodb": "^4.2.1",
    "save": "^2.4.0",
    "sqrtledger-core": "^1.0.0-alpha.40"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "prettier": "^2.5.0",
    "tslib": "^2.3.1",
    "typescript": "^4.5.2"
  }
}
