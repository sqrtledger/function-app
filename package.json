{
  "name": "sqrt-ledger-function-app",
  "version": "1.0.0",
  "description": "The Open-Source API-First Ledger",
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write --config prettier.config.js --list-different **/*.ts",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "watch": "tsc -w"
  },
  "dependencies": {
    "joi": "^17.4.2",
    "mongodb": "^4.1.4",
    "sqrtledger-core": "^1.0.0-alpha.27"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "prettier": "^2.4.1",
    "tslib": "^2.3.1",
    "typescript": "^4.4.4"
  }
}
